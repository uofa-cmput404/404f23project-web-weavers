
/* TODO: content not there yet */

export default function PostItem({props}) {

    return(
        <div style={styles.container}>
            <h1>Post Item</h1>
        </div>
    );
}

const styles = {
    container:{
        backgroundColor: 'white',
        borderRadius: '5px',
        width: "90%",
        padding: "30px",
        margin: "auto",
        marginTop: "20px",
        boxShadow:"1px 2px 2px 1px rgba(0,0,0,0.5) inset",
    }
}